package onboarding;

public class Problem4 {
    /**
     [기능목록]
     1. 문자열을 문자 배열로 만들기
     2. 소문자인 경우 반대로 변환하여 대문자로 만들기
     3. 대문자인 경우 반대로 변환하여 소문자로 만들기
     4. 알파벳 외의 문자는 변환하지 않게 하기
     */
    public static String solution(String word) {
        String answer = "";
        StringBuilder sb = new StringBuilder();

        // 문자열을 문자 배열로 만들기
        for(char c : word.toCharArray()) {
            
        }

        answer = sb.toString();
        return answer;
    }
}
