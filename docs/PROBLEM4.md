## 🚀 기능 목록

1. 인자로 받은 문자열(`word`)에 대한 유효성 검사를 실시한다.
    1. `word`의 길이는 1이상 1000이하여야 한다.
2. `word`에 대한 유효성 검사가 끝났다면, 문자열을 문제의 조건에 맞게 변화시킨다.
    1. 소문자 `a + z`의 int 값의 합은 219, 대문자 `A + Z`의 int 값의 합은 155임을 활용