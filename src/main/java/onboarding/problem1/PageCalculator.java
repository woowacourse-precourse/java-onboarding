package onboarding.problem1;

import java.util.List;

public class PageCalculator {

    public int getResult(List<Integer> page) {
        // page를 받아 계산 결과를 리턴한다.

        return -1;
    }

    private int calculatePage(int page) {
        // page를 받아 값을 계산한다.

        return -1;
    }
}
