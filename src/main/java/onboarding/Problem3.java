package onboarding;

public class Problem3 {
    // 1. 예외처리 : 1~10000 사이의 숫자인지 체크
    public static boolean isException(int number){
        if(number >= 1 && number<= 10000){
            return true;
        }
        return false;
    }
    public static int solution(int number) {
        int answer = 0;

        // 2. 받은 숫자가 들어가는 횟수 체크하는 함수 만들기.
        return answer;
    }
}
