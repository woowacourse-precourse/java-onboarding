package onboarding;

// 기능 목록
// 1. 문자열을 분석하기 위해 문자열을 배열로 변환한다.
// 2. 배열에서 연속된 중복된 문자들을 제거한다.
// 3. 배열을 다시 문자열로 변환한 뒤 결과를 반환한다.

public class Problem2 {
    public static String solution(String cryptogram) {
        String answer = "answer";
        return answer;
    }
}
