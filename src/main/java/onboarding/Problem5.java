package onboarding;

import java.util.Collections;
import java.util.List;

public class Problem5 {
    public static List<Integer> solution(int money) {
        // 액면가가 큰 화폐 순으로 최대한 많이 담기
        // 오만원권, 만원권, 오천원권, 천원권, 오백원동전, 백원 동전, 오십원 동전, 십원 동전, 일원 동전 순
        // 메서드가 카운트 값을 매개변수로 받아오면 카운트 숫자에 따른 액면가 조정
        // 길이가 2인 int 배열을 리턴 0번 인덱스에는 화폐 수량, 1번 인덱스에는 화폐로 변환 뒤 남은 금액
        // 리턴 받은 수량은 제출 할 답안 리스트에 추가
        // 리턴 받은 남은 금액은 money 변수에 다시 저장
        List<Integer> answer = Collections.emptyList();
        return answer;
    }
}
