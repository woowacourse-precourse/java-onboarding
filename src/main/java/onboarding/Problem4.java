package onboarding;

public class Problem4 {
    public static String solution(String word) {
        String answer = "";

        // 거꾸로 변환된 청개구리 문자열을 얻어 반환한다.
        // implement
        return answer;
    }

    static String getReversedString(String word) {
        // implement

        /* start of loop */

        // 만약 대문자라면 ? ( 65 <= 아스키코드 <= 90 )

        // 만약 소문자라면 ? ( 97 <= 아스키코드 <= 122 )

        // 다른 종류의 문자라면 ? -> 그대로 추가


        /* end of loop */

        return "";
    }
}
